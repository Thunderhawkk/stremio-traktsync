// Minimal entry just to pull CSS (and optional AOS init)
import './landing.css';
// Optional: import AOS CSS/JS via ESM-compatible libs or via <link> in landing.html
// import AOS from 'aos';
// import 'aos/dist/aos.css';
// AOS.init({ duration: 700, once: true, offset: 60 });
